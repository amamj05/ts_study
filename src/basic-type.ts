
let age:number = 30;
let nondisclosure:boolean = true;
let arr:number[] = [1,2,3];
let arr2:Array<number> = [1,2,3];
let arr3:Array<string> = ["일","이","삼"];

let bTuple:[string, number];
bTuple = ['z', 1];

bTuple[0].toLowerCase();
// bTuple[1].toLowerCase(); 타입에 따라 사용가능, 불가능 에러 채킹해줌
